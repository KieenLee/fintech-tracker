# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy ASP.Net Core app to Azure Web App - Fintechtracker

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      # Build frontend
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: Install frontend dependencies
        run: |
          cd fintech-tracker/fintechtracker-frontend
          npm install

      - name: Build frontend
        run: |
          cd fintech-tracker/fintechtracker-frontend
          npm run build

      # Copy frontend build to backend wwwroot
      - name: Copy frontend to backend wwwroot
        run: |
          xcopy /E /I /Y fintech-tracker\fintechtracker-frontend\dist fintech-tracker\fintechtracker-backend\wwwroot

      # Build and publish backend
      - name: Setup .NET Core
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.x'

      - name: Build backend
        run: dotnet build fintech-tracker/fintechtracker-backend/fintechtracker-backend.csproj

      - name: Publish backend
        run: dotnet publish fintech-tracker/fintechtracker-backend/fintechtracker-backend.csproj -c Release -o fintech-tracker/publish

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: .net-app
          path: fintech-tracker/publish

  deploy:
    runs-on: windows-latest
    needs: build
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: .net-app

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_7ED82C5551834B1B88C2F7CC933E135C }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_CC94A2FC302D4754AD3D59737CF9CE2F }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_9AB6A541104843E6ACE33E50195E5C90 }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'Fintechtracker'
          slot-name: 'Production'
          package: .
